# SSH Configuration for devcontainer
# Add this file to .gitignore!

# Your public key (the contents of ~/.ssh/id_ed25519.pub)
# Example: ssh-ed25519 AAAAC3NzaC1lZDI1... user@host
SSH_AUTHORIZED_KEY=ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA... your-key-here

# SSH host private key (to prevent known_hosts warnings on rebuild)
# Generate once with: ssh-keygen -t ed25519 -f ./temp_host_key -N ""
# Then paste the contents here with newlines replaced by |
# Example: -----BEGIN OPENSSH PRIVATE KEY-----|b3BlbnNzaC1rZXkt...|...|-----END OPENSSH PRIVATE KEY-----|
#SSH_HOST_ED25519_KEY=-----BEGIN OPENSSH PRIVATE KEY-----|paste-your-key-here-with-pipes-for-newlines|-----END OPENSSH PRIVATE KEY-----|
